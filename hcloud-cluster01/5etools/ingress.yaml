apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: 5etools
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: etools
            port:
              number: 80
