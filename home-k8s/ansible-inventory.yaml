all:
  vars:
    all_k3s:
      cluster_domain: home-k8s.sigurs.io
      cluster_cidr: "10.42.0.0/16"
      service_cidr: "10.43.0.0/16"


k3s_servers:
  hosts:
    home-k8s01.sigurs.io:
      k3s:
        node_labels:
          - "sigurs.io/node-type=rpi5"
