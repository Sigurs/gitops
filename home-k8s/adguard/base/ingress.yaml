apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: adguard
spec:
  rules:
  - host: pihole.sigurs.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: adguard-http
            port:
              name: http-webui
