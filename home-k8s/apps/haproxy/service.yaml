apiVersion: v1
kind: Service
metadata:
  name: haproxy-ollama
spec:
  selector:
    app.kubernetes.io/name: haproxy-ollama
  ports:
  - name: http
    protocol: TCP
    port: 11434
    targetPort: ollama