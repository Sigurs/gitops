apiVersion: apps/v1
kind: Deployment
metadata:
  name: haproxy
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: haproxy
  replicas: 1
  revisionHistoryLimit: 2
  template:
    metadata:
      labels:
        app.kubernetes.io/name: haproxy
    spec:
      automountServiceAccountToken: false
      hostUsers: false
      containers:
        - name: haproxy
          image: haproxy:lts-alpine@sha256:e26aea569336821ad17a0757de90eb688bab448a5362f39d4bb6c4275d7d8202
          securityContext:
            allowPrivilegeEscalation: false
            seccompProfile:
              type: RuntimeDefault
            capabilities:
              drop:
                - ALL
          ports:
            - containerPort: 11434
              name: ollama
          volumeMounts:
            - name: config
              mountPath: /usr/local/etc/haproxy/haproxy.cfg
              subPath: haproxy.cfg
      volumes:
      - name: config
        configMap:
          name: haproxy